server:
  port: 8762

spring:
  application:
    name: gateway-service
  cloud:
    config:
      uri: http://config-server:9090
      fail-fast: true
      label: docker-compose
  profiles:
    active: dc